# Auto detect text files and perform LF normalization
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
*.js text
*.html text
*.css text
*.md text
*.json text
*.txt text

# Declare files that will always have CRLF line endings on checkout.
# (if you need any specific files to remain CRLF)

# Declare files that will always have LF line endings on checkout.
# (uncomment if you want to force LF for all text files)
# * text eol=lf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.mov binary
*.mp4 binary
*.mp3 binary
*.flv binary
*.fla binary
*.swf binary
*.gz binary
*.zip binary
*.7z binary
*.ttf binary
*.eot binary
*.woff binary
*.woff2 binary
*.exe binary
*.pyc binary
*.pdf binary